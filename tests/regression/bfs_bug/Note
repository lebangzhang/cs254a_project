There's a bug 

If originally src0, src1, src2 
Then compile + run on build 

Then add src3 into it 
Then compile ==> The dst result will be -116sth

To Fix:
    Need to edit the kernel.cpp file 
    ==> Change:

        dst[tid] = node[index]; to dst[tid] = node[0]; 

    ==> Recompile 
    ==> Then change back 
